# Configuration file for DataZone CI/CD deployment - Dev Account 198737698272
# Stage: dev

# Service Endpoints (optional - only for non-prod environments)
service_endpoints:
  datazone: https://iceland-gamma.us-east-1.api.aws
  airflow_serverless: https://overdrive-gamma.us-east-1.api.aws

# Multi-region configuration
regions:
  primary:
    name: us-east-1
    enabled: true
  # secondary:
  #   name: us-west-2
  #   enabled: false
  # tertiary:
  #   name: us-east-1
  #   enabled: false

# DataZone Domain Tags (for auto-discovery - all tags must match)
domain_tags:
  purpose: smus-cicd-testing

# Project Configuration
projects:
  dev:
    name: dev-marketing
    description: Development environment for marketing team

# User Configuration
users:
  admin_username: amirbo
  owners:
    - amirbo
  owner_roles:
    - Admin
    - GitHubActionsRole-SMUS-CLI-Tests
