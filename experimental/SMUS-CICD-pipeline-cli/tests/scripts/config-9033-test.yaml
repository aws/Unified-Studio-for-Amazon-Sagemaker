# Configuration file for DataZone CI/CD deployment - Test Account
# Stage: test

# Multi-region configuration
regions:
  primary:
    name: us-east-1
    enabled: true

# DataZone Domain Tags (for auto-discovery - all tags must match)
domain_tags:
  purpose: smus-cicd-testing

# Project Configuration
projects:
  test:
    name: test-marketing
    description: Test environment for marketing team

# User Configuration
users:
  admin_username: amirbo
  owners:
    - amirbo
  owner_roles:
    - Admin
    - GitHubActionsRole-SMUS-CLI-Tests
