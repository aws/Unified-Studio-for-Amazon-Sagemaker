# Domain Account Configuration
# Run in account: 994753223772 (amirbo+3)
# Only the minimum needed — IDs are resolved automatically by scripts.

region: us-east-2
domain_account_id: "994753223772"
domain_id: "dzd-4h7jbz76qckoh5"

# Org Admin account ID — needed to construct the AccountCreation role ARN.
# This is the AWS Organizations management account.
org_admin_account_id: "495869084367"

domain_name: "domain-03-06-2026-164120"
# domain_id: dzd-4h7jbz76qckoh5

project_role:
  enabled: true
  role_name: AmazonSageMakerProjectRole
  managed_policy_arn: arn:aws:iam::aws:policy/SageMakerStudioAdminIAMPermissiveExecutionPolicy

pools:
  - name: default
    min_size: 2
    target_size: 5
    reclaim_strategy: REUSE
    default_project_owner: "analyst1-amirbo"
    project_profile_name: "All Capabilities - Account Pool"
